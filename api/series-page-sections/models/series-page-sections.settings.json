{
  "kind": "collectionType",
  "collectionName": "series_page_sections",
  "info": {
    "name": "Series page sections"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "products": {
      "collection": "product"
    },
    "cardType": {
      "type": "enumeration",
      "enum": [
        "wide",
        "long"
      ],
      "required": true,
      "default": "long"
    }
  }
}
