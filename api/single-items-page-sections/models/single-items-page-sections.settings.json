{
  "kind": "collectionType",
  "collectionName": "single_items_page_sections",
  "info": {
    "name": "Single Items Page Sections"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "products": {
      "collection": "product"
    },
    "cardType": {
      "type": "enumeration",
      "enum": [
        "wide",
        "long"
      ],
      "required": true,
      "default": "long"
    }
  }
}
